server:
  port: 9009

spring:
  application:
    name: notification-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/master

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    enabled: true