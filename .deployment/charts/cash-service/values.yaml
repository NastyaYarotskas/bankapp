replicaCount: 1

image:
  repository: bankapp-cash-service
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 9002

serviceAccount:
  create: true
  automount: true
  annotations: { }
  name: ""

ingress:
  enabled: true
  className: "nginx"
  annotations: {}
  hosts:
    - host: cash-service.test.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

environment:
  KEYCLOAK_ISSUE_URL: http://keycloak:8080/realms/master
  ACCOUNT_SERVICE_URL: http://{{ .Release.Name }}-accounts-service:9001
  BLOCKER_SERVICE_URL: http://{{ .Release.Name }}-blocker-service:9008
  KAFKA_URL: kafka.kafka.svc.cluster.local:9092

resources: {}

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80